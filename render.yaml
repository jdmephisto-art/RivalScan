services:
  - type: web
    name: competitor-tech-intelligence
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    plan: standard
    healthCheckPath: /api/health
